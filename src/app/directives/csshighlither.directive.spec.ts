import { CsshighlitherDirective } from './csshighlither.directive';

describe('CsshighlitherDirective', () => {
  it('should create an instance', () => {
    const directive = new CsshighlitherDirective();
    expect(directive).toBeTruthy();
  });
});
