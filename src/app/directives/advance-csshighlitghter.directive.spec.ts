import { AdvanceCsshighlitghterDirective } from './advance-csshighlitghter.directive';

describe('AdvanceCsshighlitghterDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvanceCsshighlitghterDirective();
    expect(directive).toBeTruthy();
  });
});
